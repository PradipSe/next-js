const Router = require('koa-router')

const router = new Router({
	prefix: '/api',
})

// Add API routes here for eg: 

// router.get('/articles', async ctx => performAPICall(ctx))


module.exports = router